import _ from 'lodash'
import React, { PropTypes } from 'react'

const propTypes = {
}

const ConfigEditor = () => {
  return <div>Config Editor</div>
}

ConfigEditor.propTypes = propTypes

export default ConfigEditor
